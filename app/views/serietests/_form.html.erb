<%= form_with(model: serietest) do |form| %>
  <% if serietest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(serietest.errors.count, "error") %> prohibited this serietest from being saved:</h2>

      <ul>
        <% serietest.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :genre_id %>
    <%= form.collection_select(:genre_id, Genre.all, :id, :name) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
